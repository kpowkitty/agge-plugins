package com.aggeplugins.lib;

import com.aggeplugins.lib.*;
import com.aggeplugins.lib.export.*;

import java.util.*;

public class Random {
    public static int getRandIdx(List<T> l)
    {
        return random.nextInt(l.size());
    }
}
